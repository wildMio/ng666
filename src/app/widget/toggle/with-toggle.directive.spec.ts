import { WithToggleDirective } from './with-toggle.directive';

describe('WithToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new WithToggleDirective();
    expect(directive).toBeTruthy();
  });
});
